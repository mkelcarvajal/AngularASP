import { EstadoPipePipe } from './estado-pipe.pipe';

describe('EstadoPipePipe', () => {
  it('create an instance', () => {
    const pipe = new EstadoPipePipe();
    expect(pipe).toBeTruthy();
  });
});
