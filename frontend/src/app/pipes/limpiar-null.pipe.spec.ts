import { LimpiarNullPipe } from './limpiar-null.pipe';

describe('LimpiarNullPipe', () => {
  it('create an instance', () => {
    const pipe = new LimpiarNullPipe();
    expect(pipe).toBeTruthy();
  });
});
